import 'package:flutter/material.dart';
import 'package:theme_generate/core/themes/colors.dart';

const double _iconSize = 30.0;

const Icon userIcon = Icon(
  Icons.person,
  size: _iconSize,
  color: mainThemeColor,
);
